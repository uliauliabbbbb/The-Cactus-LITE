# 🌵 The Cactus App - Помощник для фокусировки студентов

## 📋 Описание проекта
**The Cactus App** — это консольное приложение для управления временем и повышения продуктивности студентов, использующее геймификацию через выращивание виртуальных кактусов. Каждая успешная сессия фокусировки выращивает уникальный кактус, создавая мотивацию для регулярных учебных сессий.

## 🎯 Основные функции

### ✅ Управление сессиями фокусировки
- **Запуск сессий** с заданной длительностью (в минутах)
- **Два режима завершения**: успешное завершение или прерывание
- **Автоматический подбор кактуса** в зависимости от длительности сессии и текущего стрика

### 🌵 Система кактусов
- **Три типа кактусов** с уникальной графикой:
  - **Базовый кактус** — для обычных сессий
  - **Редкий кактус** — для сессий длительностью 60+ минут
  - **Событийный кактус** — для пользователей с активным стриком (5+ дней подряд)
- **Три состояния каждого кактуса**:
  - 🌱 Растущий
  - 🌵 Выросший
  - 💀 Засохший (при прерванной сессии)

### 📊 Статистика и прогресс
- Общее время фокусировки
- Количество успешных и прерванных сессий
- Самая длинная сессия
- Текущий стрик (дней подряд)
- История всех сессий

### 🏡 Кактусовый сад
- Просмотр всех выращенных кактусов
- Разделение на живые и засохшие кактусы
- Графика для каждого типа кактуса

## 🚀 Быстрый старт

### Требования
- .NET SDK 6.0 или выше
- Консоль с поддержкой UTF-8 (для отображения эмодзи)

### Установка и запуск
```bash
# 1. Клонирование репозитория
git clone <repository-url>
cd TheCactusApp

# 2. Компиляция и запуск
dotnet run
```

## 🎮 Как пользоваться

### Основной рабочий цикл:
1. **Введите ваше имя** при первом запуске
2. **Выберите действие** в главном меню:
   - `1` — Начать новую сессию фокусировки
   - `2` — Посмотреть сад кактусов
   - `3` — Показать статистику
   - `4` — Выйти из приложения

### Запуск сессии:
1. Выберите "Начать новую сессию"
2. Введите длительность в минутах
3. Кактус автоматически выбирается:
   - **Базовый** — < 60 минут
   - **Редкий** — ≥ 60 минут
   - **Событийный** — при стрике ≥ 5 дней
4. В демо-режиме сессия завершается сразу
5. Нажмите:
   - `Enter` — успешное завершение (кактус растет)
   - `q` + `Enter` — прерывание (кактус засыхает)

## 🏗️ Архитектура проекта

### Основные классы:

#### 1. **Cactus (абстрактный класс)**
```csharp
public abstract class Cactus
{
    // Базовый класс для всех кактусов
    // Содержит: имя, время роста, статус, методы роста
}
```

#### 2. **Типы кактусов:**
- `BasicCactus` — базовый кактус
- `RareCactus` — редкий кактус для длинных сессий
- `EventCactus` — событийный кактус для стриков

#### 3. **FocusSession**
```csharp
public class FocusSession
{
    // Управление отдельной сессией фокусировки
    // Связывает время сессии с конкретным кактусом
}
```

#### 4. **SessionManager**
```csharp
public class SessionManager
{
    // Управление жизненным циклом сессий
    // Автоматический подбор типа кактуса
}
```

#### 5. **User**
```csharp
public class User
{
    // Пользователь с садом кактусов и статистикой
    // Центральный класс для взаимодействия
}
```

#### 6. **Statistics**
```csharp
public class Statistics
{
    // Сбор и отображение статистики
    // Учет стриков и рекордов
}
```

#### 7. **CactusGarden**
```csharp
public class CactusGarden
{
    // Коллекция всех кактусов пользователя
    // Фильтрация по состоянию (живые/засохшие)
}
```

## 📁 Структура проекта
```
Program.cs
├── Перечисления (Enums)
│   ├── CactusStatus
│   └── SessionStatus
├── Классы кактусов
│   ├── Cactus (абстрактный)
│   ├── BasicCactus
│   ├── RareCactus
│   └── EventCactus
├── Менеджмент
│   ├── FocusSession
│   ├── SessionManager
│   ├── CactusGarden
│   └── Statistics
├── Пользователь
│   └── User
└── Главная программа
    └── Main() с меню
```

## 🌈 Особенности реализации

### 1. **Геймификация**
- Визуальное представление прогресса через кактусы
- Разные типы кактусов как награда за достижения
- Система стриков для поддержания мотивации

### 2. **ООП-принципы**
- Наследование (абстрактный класс Cactus)
- Инкапсуляция (private поля с public свойствами)
- Полиморфизм (метод Render() в каждом классе кактуса)

### 3. **Пользовательский интерфейс**
- Интуитивное консольное меню
- Цветное форматирование вывода
- Поддержка эмодзи и ASCII-арта

### 4. **Обработка данных**
- Валидация ввода пользователя
- Учет временных меток
- Статистические расчеты

## 🔧 Технические детали

### Кодировка и эмодзи
```csharp
// Для корректного отображения эмодзи
Console.OutputEncoding = System.Text.Encoding.UTF8;
```

### Состояния кактуса
```csharp
public enum CactusStatus
{
    Growing,    // 🌱 Растущий
    Grown,      // 🌵 Выросший
    Withered    // 💀 Засохший
}
```

### Логика выбора кактуса
```csharp
if (streakDays >= 5)
{
    cactus = new EventCactus(duration);
}
else if (duration >= 60)
{
    cactus = new RareCactus(duration);
}
else
{
    cactus = new BasicCactus(duration);
}
```

## 🚀 Возможные улучшения

### Краткосрочные:
1. **Реальный таймер** с обратным отсчетом
2. **Сохранение данных** между запусками (JSON/XML)
3. **Больше типов кактусов** с уникальными условиями
4. **Система достижений** и бейджей

### Долгосрочные:
1. **Веб-версия** с облачной синхронизацией
2. **Мобильное приложение** с уведомлениями
3. **Социальные функции** — сравнение с друзьями
4. **Кастомизация** — цветы, горшки для кактусов

## 🤝 Вклад в проект

### Как помочь:
1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Запушьте изменения
4. Создайте Pull Request

### Области для вклада:
- Добавление новых типов кактусов
- Улучшение графики (ASCII-арт)
- Оптимизация кода
- Исправление багов
- Перевод на другие языки


## 👏 Благодарности
- Идея вдохновлена техниками Pomodoro и приложениями для трекинга привычек (Forest)
- Спасибо всем, кто тестировал и предлагал улучшения!

---
**🌵 Выращивайте кактусы, растите сами!** 🚀

*Приложение создано для помощи студентам в поддержании фокуса и дисциплины в учебном процессе.*
